rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARY RULES FOR INITIAL DATA MIGRATION
    // Allow all reads and writes temporarily
    // IMPORTANT: Replace with secure rules after migration!
    
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
