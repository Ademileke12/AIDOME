rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARY: Allow all writes for data migration
    // IMPORTANT: Deploy the secure rules after migration!
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
